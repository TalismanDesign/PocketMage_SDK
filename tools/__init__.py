"""PocketMage SDK developer tooling package.

Makes the ad-hoc scripts importable as `tools.symbols` and hosts the `pm`
developer CLI under `tools.pm`. Run `pm` with `python3 -m tools.pm` from the
SDK root, or `pip install .` for a `pm` command on PATH.
"""